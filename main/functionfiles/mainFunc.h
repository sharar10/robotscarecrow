#ifndef mainFunc_hpp
#define mainFunc_hpp

#include <stdio.h>

class MainFunc {
public:
    bool timingFunc();
	int fieldStatus();
	bool hangarDoor(bool);
	bool mission(bool);
};

#endif /* mainFunc_hpp */